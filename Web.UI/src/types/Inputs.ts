export type InputTypes = {
    name: string;
    type: string;
    label: string;
    disabled?: boolean;
    required?: boolean;
    dontDisplay?: boolean;
    placeholder?: string;
    autocomplete?: [];
    noLabel?: boolean;
}